#' @param rate Rate of distribution (default 1)
#'
#' @details
#'      The exponential distribution with rate \eqn{\lambda} has density
#'
#'          \deqn{f(x) = \lambda e^{-\lambda x}}{%
#'                f(x) = \lambda e^(-\lambda x)}
#'
#'      for \eqn{x \geq 0}.
#'
#' @seealso \code{\link[=rexp]{stats::rexp}}
